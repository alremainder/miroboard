services:
  - type: web
    name: board-app
    env: node
    plan: starter
    region: oregon
    buildCommand: rm -rf node_modules && npm install
    startCommand: node server.js
    healthCheckPath: /health
    disk:
      name: board-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /data
      - key: SESSION_SECRET
        generateValue: true
      - key: RESEND_API_KEY
        sync: false
      - key: MAIL_FROM
        sync: false
